<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- Title (top-left) -->
    <TextView
        android:id="@+id/appTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Profile Picture Updater"
        android:textColor="#2D2D2D"
        android:textSize="22sp"
        android:textStyle="bold"
        android:layout_margin="24dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Profile Image (left column, centered vertically) -->
    <ImageView
        android:id="@+id/pfPic"
        android:layout_width="260dp"
        android:layout_height="260dp"
        android:src="@drawable/placeholder"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toBottomOf="@id/appTitle"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="24dp"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="24dp" />

    <!-- Right panel: buttons + status stacked vertically -->
    <LinearLayout
        android:id="@+id/rightPanel"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="24dp"
        app:layout_constraintTop_toTopOf="@id/pfPic"
        app:layout_constraintBottom_toBottomOf="@id/pfPic"
        app:layout_constraintStart_toEndOf="@id/pfPic"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Buttons row -->
        <LinearLayout
            android:id="@+id/buttonRow"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginBottom="24dp">

            <Button
                android:id="@+id/takeButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Take Photo"
                android:textColor="#FFFFFF"
                android:backgroundTint="#8A7EFF"
                android:paddingHorizontal="24dp"
                android:paddingVertical="10dp"
                android:elevation="4dp"
                android:layout_marginEnd="16dp" />

            <Button
                android:id="@+id/selectButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Select From Gallery"
                android:textColor="#FFFFFF"
                android:backgroundTint="#8A7EFF"
                android:paddingHorizontal="24dp"
                android:paddingVertical="10dp"
                android:elevation="4dp" />
        </LinearLayout>

        <!-- Status text -->
        <TextView
            android:id="@+id/statusText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="16sp"
            android:textColor="#4B4B4B"
            tools:text="Selected image from gallery." />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
